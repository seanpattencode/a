# Secrets & credentials
*.env
.env*
*credentials*
*secret*
*token*

# Python cache
__pycache__/
*.pyc
*.pyo

# Databases
*.db
*.sqlite
*.sqlite3

# Local config
.local/

.aider*
.a_done
.a_job
.claude/
/my
/lab

# User data — adata/ is a separate world from the code repo.
# adata/git/ is its own independent git repo (not a submodule).
# .gitignore makes the parent repo completely blind to it.
adata/

# Meta-agent: own gh repo, at root for easy access, blind to main repo
/m/

# C build artifacts
*.o
*.plist
*.so
infer-out/
a
a.dmg
a.deb
/win

# macOS Finder metadata
.DS_Store
lib/bri-ext/*.xpi

# dedup: bri-chrome copy restored from bri-ext by `a bri serve` (tok cap)
lib/bri-chrome/instant-preload.js
